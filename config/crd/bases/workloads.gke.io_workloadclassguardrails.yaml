---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.20.1
  name: workloadclassguardrails.workloads.gke.io
spec:
  group: workloads.gke.io
  names:
    kind: WorkloadClassGuardrail
    listKind: WorkloadClassGuardrailList
    plural: workloadclassguardrails
    singular: workloadclassguardrail
  scope: Cluster
  versions:
  - name: v1
    schema:
      openAPIV3Schema:
        description: WorkloadClassGuardrail is the Schema for the workloadclassguardrails
          API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: spec defines the desired state of WorkloadClassGuardrail
            properties:
              constraints:
                description: Constraints defines the guardrails for WorkloadClasses.
                properties:
                  disruption:
                    description: Disruption defines the constraints within which WorkloadClasses
                      can set disruption policies.
                    properties:
                      allowedDisruptionDays:
                        description: AllowedDisruptionDays specifies days on which
                          disruption can happen (Monday-Sunday).
                        items:
                          type: string
                        type: array
                      emergencyOverride:
                        description: EmergencyOverride allows bypassing all constraints
                          immediately.
                        type: boolean
                      enforcedDisruptionTimeoutSeconds:
                        description: EnforcedDisruptionTimeoutSeconds is the maximum
                          time in seconds before a disruption is forced.
                        format: int32
                        maximum: 3600
                        type: integer
                      maxAllowedWindows:
                        description: |-
                          MaxAllowedWindows sets the limit of how many windows workload owners can set.
                          This avoid complications where windows are too short or cases where there could be too many disruptions to workloads.
                        format: int32
                        minimum: 1
                        type: integer
                      maxNonDisruptionDurationDays:
                        description: MaxNonDisruptionDurationDays is the limit for
                          how long workload owners can go without having an open maintenance
                          window.
                        format: int32
                        minimum: 1
                        type: integer
                    required:
                    - maxNonDisruptionDurationDays
                    type: object
                  placement:
                    default: {}
                    description: Placement specifies compute capacity and scheduling
                      guardrails for workloads.
                    properties:
                      spotPlacement:
                        default: {}
                        description: SpotPlacement defines guardrails and lifecycle
                          rules for Spot capacity usage.
                        properties:
                          enforcementMode:
                            default: Allowed
                            description: |-
                              EnforcementMode enforces Spot usage. Required forces Spot. Forbidden blocks Spot.
                              Allowed lets workload choose.
                            enum:
                            - Allowed
                            - Required
                            - Forbidden
                            type: string
                          fallback:
                            default:
                              allowFallbackToOnDemand: true
                            description: Fallback controls On-Demand fallback behavior
                              when Spot capacity is unavailable.
                            properties:
                              allowFallbackToOnDemand:
                                default: true
                                description: AllowFallbackToOnDemand specifies if
                                  workload owners are wllowed to fallback to On-Demand.
                                type: boolean
                              maxFallbackRatio:
                                anyOf:
                                - type: integer
                                - type: string
                                description: |-
                                  MaxFallbackRatio is the budget control specifying the maximum percentage (e.g. "25%")
                                  or absolute number (e.g. 5) of pods in the class allowed on On-Demand fallback concurrently.
                                x-kubernetes-int-or-string: true
                                x-kubernetes-validations:
                                - message: must be a non-negative integer or a percentage
                                    between 0% and 100%
                                  rule: 'type(self) == int ? self >= 0 : self.matches(''^(100|[1-9]?[0-9])%$'')'
                            type: object
                            x-kubernetes-validations:
                            - message: maxFallbackRatio cannot be set when allowFallbackToOnDemand
                                is false
                              rule: self.allowFallbackToOnDemand || !has(self.maxFallbackRatio)
                          minSpotRatio:
                            description: |-
                              MinSpotRatio is the minimum allowed Spot ratio (e.g., "50%").
                              Prevents workload owners from configuring too low Spot ratios.
                            pattern: ^(100|[1-9]?[0-9])%$
                            type: string
                          reversion:
                            description: Reversion controls the transition of fallback
                              workloads back to Spot capacity.
                            properties:
                              maxFallbackDuration:
                                description: |-
                                  MaxFallbackDuration sets an upper bound on how long a workload can run on
                                  fallback before GKE forces reversion (disruption) (e.g., "30m", "2h").
                                type: string
                                x-kubernetes-validations:
                                - message: maxFallbackDuration must be greater than
                                    0
                                  rule: duration(self) > duration('0s')
                              requiredReversionAction:
                                description: RequiredReversionAction enforces a reversion
                                  strategy, e.g., must be `Active`.
                                enum:
                                - Active
                                - Lazy
                                - None
                                type: string
                            type: object
                        type: object
                        x-kubernetes-validations:
                        - message: minSpotRatio cannot be set when enforcementMode
                            is Forbidden
                          rule: self.enforcementMode != 'Forbidden' || !has(self.minSpotRatio)
                    type: object
                required:
                - disruption
                type: object
            required:
            - constraints
            type: object
          status:
            description: status defines the observed state of WorkloadClassGuardrail
            properties:
              conditions:
                description: |-
                  conditions represent the current state of the WorkloadClassGuardrail resource.
                  Each condition has a unique type and reflects the status of a specific aspect of the resource.

                  Standard condition types include:
                  - "Available": the resource is fully functional
                  - "Progressing": the resource is being created or updated
                  - "Degraded": the resource failed to reach or maintain its desired state

                  The status of each condition is one of True, False, or Unknown.
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
