---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.20.1
  name: spotplacementconfigs.workloads.gke.io
spec:
  group: workloads.gke.io
  names:
    kind: SpotPlacementConfig
    listKind: SpotPlacementConfigList
    plural: spotplacementconfigs
    singular: spotplacementconfig
  scope: Namespaced
  versions:
  - name: v1
    schema:
      openAPIV3Schema:
        description: SpotPlacementConfig is the Schema for the spotplacementconfigs
          API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: SpotPlacementConfigSpec defines the desired state of SpotPlacementConfig
            properties:
              spotRatioPercent:
                format: int32
                type: integer
            required:
            - spotRatioPercent
            type: object
        type: object
    served: true
    storage: true
