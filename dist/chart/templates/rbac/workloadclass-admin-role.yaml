{{- if .Values.rbac.helpers.enable }}
apiVersion: rbac.authorization.k8s.io/v1
{{- if .Values.rbac.namespaced }}
kind: Role
{{- else }}
kind: ClusterRole
{{- end }}
metadata:
{{- if .Values.rbac.namespaced }}
  namespace: {{ .Release.Namespace }}
{{- end }}
  labels:
    app.kubernetes.io/managed-by: {{ .Release.Service }}
    app.kubernetes.io/name: {{ include "workload-class.name" . }}
    helm.sh/chart: {{ .Chart.Name }}-{{ .Chart.Version | replace "+" "_" }}
    app.kubernetes.io/instance: {{ .Release.Name }}
  name: {{ include "workload-class.resourceName" (dict "suffix" "workloadclass-admin-role" "context" $) }}
rules:
- apiGroups:
  - workloads.gke.io
  resources:
  - workloadclasses
  verbs:
  - '*'
- apiGroups:
  - workloads.gke.io
  resources:
  - workloadclasses/status
  verbs:
  - get
{{- end }}
